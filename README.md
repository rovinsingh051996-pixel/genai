# genai
Q&amp;A Chatbot with Memory

step 1: run following command 

!pip install -q langchain langchain-core langchain-community langchain-groq langchain-text-splitters
!pip install -q faiss-cpu sentence-transformers

step 2: run entire code and provide groq api key 

Step 3: Ask one question at a time.
If you have multiple queries, separate them with a comma so that each can be answered individually.
Example: What is Gen AI, Gen AI vs Agentic AI
